server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Allow OAuth popups (Google) to postMessage back to opener
  add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;

  # Serve static assets with long cache
  location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
    try_files $uri =404;
    add_header Cache-Control "public, max-age=31536000, immutable";
    add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
  }

  # PWA files (no long cache for sw)
  location = /sw.js { try_files $uri =404; add_header Cache-Control "no-cache"; add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always; }
  location = /manifest.webmanifest { try_files $uri =404; }

  # SPA fallback for client-side routing
  location / {
    try_files $uri $uri/ /index.html;
    add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
  }
}
